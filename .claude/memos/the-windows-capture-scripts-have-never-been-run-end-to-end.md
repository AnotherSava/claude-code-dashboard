---
created: 2026-09-13 11:20:00
---

# The Windows capture scripts have never been run end to end since they were rewritten

Six `.ps1` files under `docs/screenshots/capture/` were edited across 2026-09-11..13 — `Add-Hairline` was added to `lib/dashboard.ps1` and wired into all five capture scripts — and **not one of them has produced a frame since**. What has been done is everything short of that: parse-checked twice under both Windows PowerShell 5.1 and pwsh 7, confirmed all five callers dot-source the lib so `Add-Hairline` resolves, the function exercised against a nonexistent path so its guards and its `$suggest` string were seen to render, and `hairline.py` itself run directly on committed blobs (refusal case, idempotency, the `--opaque --require` path, the PNG round-trip). All passed.

None of that runs the capture. So these remain unobserved on Windows:

- `Add-Hairline -Opaque` stroking a **freshly captured** frame rather than a committed blob — the real path takes `Invoke-WindowShot`'s output, which is alpha-keyed from two exposures, not a PNG read off disk.
- The `terminal-tabs` ordering, where the stroke must land on the **crop** and not on the window it was cropped from.
- `history-window-windows.ps1`'s `if ($Method -eq 'Alpha')` guard, which is supposed to keep a PrintWindow probe from being dressed up as a committed frame.
- Whether the committed Windows frames are actually **reproducible** by these scripts. They are not currently evidence of it: every one was re-processed on the Mac (192ef92) rather than produced by the Windows capture path. The script and the artifact have never met.

**Why this is not just "run it once".** A capture takes the machine — it hides the always-on-top widget and reads the strip off the desktop — so it needs the user's approval at the moment it runs, and it re-shoots a committed figure with whatever sessions happen to be staged, which changes that figure's subject. Both are reasons the obvious verification keeps being deferred, and both are why this is a memo rather than a task that was simply skipped.

**Do it as part of the next genuine re-capture, not as a test.** The cheapest honest moment is whenever a Windows frame next needs re-shooting for its own sake: the machine is taken anyway, the subject is changing anyway, and the staging is being done anyway. Verifying then costs nothing extra. Running a capture *purely* to test the path spends the user's screen and changes a committed frame to learn something that the next real capture would have told us for free.

**Two things to check when that happens**, neither of which the parse checks can see: that the stroked frame measures 2px of `#BDBDBD` at full alpha on all four sides and composites to 189 on both a white and a dark page (matching the ten frames as they stand), and that `terminal-tabs-windows.png` gets its border on the **crop's** four sides rather than on the pre-crop window.

Related: the Windows capture path also has no `assert_publishable` guard, where the macOS side enforces the publishable-session rule in code (`dashboard.py`). That is its own gap and its own memo — but it bites in the same moment, since both are only testable when a capture actually runs. Raised by the AIR session, which reproduced a failure its own manifest entry had documented two days earlier.

UPDATE 2026-09-20: `lib/dashboard.ps1` changed since the verification inventory above was taken, so that inventory no longer describes the file. The dotfiles renamed the `documentation` skill to `docs-relevance` on 2026-09-17, and both capture libs still resolved the old path — `Add-Hairline` would have thrown `hairline.py is missing` on every Windows re-shoot, and `SKILL_SCRIPTS` in `lib/dashboard.py` the same on macOS. The `$hairline` literal in `Add-Hairline` and two comments naming the old skill were corrected, along with the matching line in `terminal-tabs-windows.ps1`. The macOS half is verified (both `skill_script()` targets resolve). The PowerShell half is NOT: it was edited from the Mac, with no `pwsh` to parse-check it. The edit is pure ASCII and the BOM is intact, which is what the lib's header warns about, but that is the whole of the evidence — none of the parse checks listed above was re-run. Re-do them at the next genuine re-capture, before trusting the guards this memo says were exercised.

UPDATE 2026-09-23: the first item above is now observed. `tray-menu-windows.ps1`, a new capture script, ran end to end on the Windows box: it dot-sourced `lib/dashboard.ps1`, took a fresh two-exposure Alpha frame through `Invoke-WindowShot` (with the new `-Popup` switch), and `Add-Hairline -Opaque` resolved the renamed `docs-relevance` path and stroked it. The frame measures 2px of `#BDBDBD` at alpha 255 on all four sides. `lib/dashboard.ps1` and `lib/window-shot.ps1` were also parse-checked again under pwsh 7 (0 errors); Windows PowerShell 5.1 was not re-run. Still unobserved: the `terminal-tabs` crop ordering, the `history-window-windows.ps1` Alpha guard, and whether the committed frames are reproducible by these scripts. When those frames are next re-shot, the widget build must include the macOS-only corner radius fix of 2026-09-23. A build between `ee0fec6` and that fix draws its own 10px radius inside Windows 11's 8px one, and every widget frame taken from it carries a see-through arc at each corner.

UPDATE 2026-09-23, later: the acceptance numbers above are superseded. Windows frames no longer get the flat opaque border: `Add-Hairline` (no `-Opaque` switch any more) runs `hairline.py --lift 0.5`, which keeps Windows' own translucent border with its soft corners and lightens it. A correct frame reads about 150-180 on a white page and 104-117 on GitHub's dark page, with the border stronger toward the bottom; it does not measure 189 at full alpha, and a check for that would fail a good frame.

UPDATE 2026-09-24: superseded again. `Add-Hairline` is gone; `Add-WindowFrame` runs the docs-relevance skill's `winframe.py`, which draws the Windows frame from DWM's own model and keeps only the content inside Windows' 2px clip. All six Windows frames were re-framed with it without re-capture. A correct frame now has a uniform 2px band of (146, 146, 146) at alpha 176 on every side, reading 180 on a white page and 105 on a dark one, with 12px corners (6px on the menu). The capture must record its DPI in the PNG (window-shot.ps1 now does) or the frame step refuses the file. Still unobserved end to end: a fresh capture through `Add-WindowFrame` itself. Later the same day the pointer park-and-restore that `tray-menu-windows.ps1` and `work-intensity-windows.ps1` each carried moved into `lib/dashboard.ps1` as `Invoke-WithPointerAt`, after both scripts had last run, so neither has run in its current form either; the helper itself was exercised in place (a throwing block still restores the pointer, and the block reads script-scope variables).

UPDATE 2026-09-25: the shutter and the generic helpers moved out of this repo. `lib/window-shot.ps1` is deleted, and `lib/dashboard.ps1` now dot-sources the docs-relevance skill's `windows-capture.ps1`, which carries `Invoke-WindowShot` (on the skill's own `window-shot.ps1`), `Add-WindowFrame`, `Get-PrimaryWorkArea`, `Invoke-WithPointerAt` and `Find-ProcessWindows`; `tray-menu-windows.ps1` uses the last in place of its own `Find-AppWindows`. Observed on the Windows box under both 5.1 and pwsh 7: every capture script parses with 0 errors, all the lib's functions resolve through the nested dot-source, `Find-ProcessWindows` finds the running dashboard's `tray_icon_app` window, and the shared `Add-WindowFrame`, called through a wrapper the way `Invoke-WithPointerAt` calls its block, kept its raw in this repo's `tmp/screenshot-raws/` and re-framed the kept tray-menu and work-intensity raws into frames pixel-identical to the committed ones. Those kept raws record no DPI, so they had to be stamped at 144 first; `winframe.py` refuses them as they are. Still unobserved: any capture since the switch, meaning the shutter itself, the pointer move, and the tray menu opened by a posted message.

UPDATE 2026-09-26: the shared shutter and frame step are now observed through a real capture. `hero-windows.ps1`, rewritten to replay `fixtures/hero.json`, ran end to end three times on the Windows box: `Invoke-DashboardFixture` restarted the app on a capture config and restored it, `Invoke-WindowShotAtPulsePeak` took fresh two-exposure Alpha frames through the skill's `Invoke-WindowShot`, and the skill's `Add-WindowFrame` framed the kept one, committed as `hero-windows.png` in c9012e9. That frame is also reproducible by its script, since its subject is the fixture rather than whatever sessions were running. Still unobserved: the pointer move (`Invoke-WithPointerAt`) and the tray menu opened by a posted message, both used only by `tray-menu-windows.ps1` and `work-intensity-windows.ps1`, the `terminal-tabs` crop ordering, and the `history-window-windows.ps1` Alpha guard.
