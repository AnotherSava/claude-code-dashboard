---
created: 2026-09-02 02:55:00
---

# re add sessions from agterm after restart [DONE: `session_restore.rs`, behind the same…

re add sessions from agterm after restart [DONE: `session_restore.rs`, behind the same `terminals::TerminalAdapter` seam as the ready→idle trigger (new `sessions()` method; Windows returns `None` and restores nothing until its adapter exists). Two oracles, each asked only what it is authoritative for: `session_registry` for *is an agent alive here* (an agterm tab outlives the agent in it, since `session_launcher` starts `zsh -ilc 'claude; exec zsh -i'`), the tab title for *what was it doing and had you read it* (`Activity` is idle/busy only — verified live, `what-is-next` read `idle` there while its tab read `✋`). The tab title turned out to be the only durable copy of a row's status, and because titles are written after `apply_read_as_idle` it round-trips read-vs-unread too, so `attended_at` needed no persisting. `Working`/`Waiting` are the two statuses a stale glyph cannot be trusted for and survive only where the registry reports `Busy`. A tab with no title we recognize is skipped, which is also the anti-resurrection guard. Adversarial review found nine real defects, all fixed — notably that a bare `agtermctl tree` sees only the frontmost window, and that a tab can carry a glyph from a *previous* conversation, now closed by requiring `ChatIdRegistry` to have anchored one of the session's ids. Verified live: 10 live sessions / 0 rows → 10 restored, `what-is-next` back as BLOCK after 4h invisible, titles round-tripped byte-identically, later scans cost nothing.]
