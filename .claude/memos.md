# Memos

Off-task ideas captured during sessions, to revisit later. Managed by `/memo`; open items resurface at session start, task completion, and commit.

- [x] 2026-06-26 03:39 — If an agent was renamed in the dashboard, use the new name for notifications
- [x] 2026-06-26 04:57 — merge usage limit stats across dashboards running on different machines
- [x] 2026-06-27 02:49 — add yellow status when background agents are running
- [x] 2026-06-27 03:11 — Dashboard doesn't reach Work state until the first agent output appears — investigate whether UserPromptSubmit should flip the row to Working immediately; check widget.jsonl decision log around a fresh request
- [x] 2026-06-27 16:16 — Wire the explicit MCP/compaction lifecycle hooks (Claude Code 2.1.x): ElicitationResult as a clean unblock signal (user answered the MCP prompt -> leave Blocked) and PostCompact as a cleaner 'compaction done' than inferring it (PreCompact already drives the boundary). Low effort, clarity-only — not structural. From the hooks/state-logic review (Tier 2). [DONE: wired ElicitationResult → resume Working (adapter + setup snippet + hook + CLAUDE.md). Skipped PostCompact — read-only, and we infer no 'compaction done' anywhere (PreCompact already marks the boundary), so it'd be a no-op Ignore.]
- [x] 2026-06-27 16:30 — Harden is_a_question (adapters/claude.rs) — single-pass rule matching (first match wins, no priority), paragraph split on \n\n only, 160-char tail-truncated evidence snippet, and empirically-grown PERMISSION_SEEKING/HANDBACK phrase lists. Now that it runs once on the authoritative Stop.last_assistant_message (no more dual stale/flushed reads), accuracy here is the whole verdict. Validate any change against prompt_history.json per the validate_detection_against_history memory; prefer phrase-matching over broad structural rules. [DONE: audited all 53 real assistant turns in the corpus — 0 false positives, 1 false negative (hand-back question in the second-to-last paragraph trailed by a 'Then I'll …' outro). Fixed with a phrase-gated TRAILING_OUTRO_OPENERS look-back; self-answered case stays Done.]
- [x] 2026-06-27 16:31 — ChatIdRegistry concurrent-write race (chat_id_registry.rs) — session_chat_ids.json is written with no atomic write/lock, so overlapping writes (e.g. two rapid hook events) can corrupt or lose the session_id->chat_id mapping. Low frequency but a real correctness gap. Fix = write-to-temp + atomic rename (and/or an in-process lock around the read-modify-write). [DONE: save_to_disk now writes a sibling .tmp then atomic-renames over the target; the mutex is held through the rename so overlapping saves stay totally ordered. The in-process lock already existed — the real gap was non-atomicity (a torn file reloads as empty and drops every mapping).]
- [x] 2026-06-27 16:31 — Use explicit SessionStart.source (startup|resume|clear|compact) and SessionEnd.reason (clear|logout|...) matchers for boundary detection instead of the transcript_path-rotation heuristic + the dormant >50% token-drop inference (state.rs is_new_session, which never fires on the hook path since the adapter doesn't populate input_tokens). source=clear/compact and reason=clear are authoritative signals now available; could simplify http_server.rs boundary handling and let us drop the dead token-drop branch. From the hooks/state-logic review. [DONE: deleted the dead token-drop branch + last_input_tokens plumbing AND the transcript-rotation heuristic; SessionEnd/PreCompact are the authoritative boundary signals. Did NOT adopt source/reason matching — redundant with those existing paths, not a simplification.]
- [x] 2026-07-04 02:13 — add notification on limit reset if more than 90 percent used [DONE: `TelegramConfig.limit_reset_percent` (default 90, on). `ResetTracker` in notifications.rs detects a 5h/7d reset by the `resets_at` forward jump (>10min), NOT a pct drop — real data showed a 5h reset transiently zeroes BOTH buckets' pct for ~40min, so a 7d pct can crater without its window resetting; `peak_pct` running-max survives the dips. One-shot fire-and-forget ("5h limit reset (was 96%)"), buffered+retried on send failure, re-seeds on creds change. Backend-only (no frontend). Validated: 29-day history replay = 16 fires / 0 false positives; adversarial review caught 4 issues (peak over-report on direct jump, send-retry, creds-toggle re-seed, doc), all fixed.]
- [x] 2026-07-23 11:22 — not a question Ready when you are what would you like to work on [DONE: added "what would you like to work on?" + "…next?" to the default `benign_closers` (config.rs) so a "what next" sign-off settles Done, not Blocked. Same family as the existing "What's next?" closer; suffix-matched. Validated against prompt_history.json — both real corpus forms ("Ready when you are — …", "I'm ready to help. …") end with the phrase and no genuine hand-back does; guard test keeps "How would you like to proceed?" awaiting.]
- [x] 2026-07-28 13:18 — auto-resize doesn't block manual resize on macOS [DONE: `auto_resize.rs` grew a platform `resize_lock` (install/engage/release) — Windows keeps the WM_NCHITTEST subclass, macOS pins min == max content height via `set_size_constraints` to the height `apply` is about to set, rebuilt from the window's tauri.conf.json bounds each time so the declared `minWidth` survives the four-bounds write. `engage` runs *before* `set_size`: a stale pin would otherwise clamp the resize, and tao applies constraints synchronously while deferring `set_inner_size` to the main queue. Every bound is spelled out (0 / f32::MAX) rather than left `None`, which tao expands to f64::MIN/f64::MAX and hands NSWindow verbatim. Verified on macOS: engaged → a 420x300 resize request left the window at 420x61 with width still free; released → the same request gave 420x300.]
- [x] 2026-07-29 23:45 — Sync watermarks are deliberately in-memory (soft state) so a wiped remote_usage/ or remote_history/ self-heals on the peer's next restart — the receiver treats delta_from == 0 / ts > held_max as "complete state" replays. But the repair is restart-triggered only: a peer running for weeks never re-advertises, so a wipe here stays unfilled until it restarts. Usage has no catch-up endpoint (dialogs have GET /api/sync/dialog?id=&since=). Fix = periodic full re-advertise (reset watermarks every N hours) or a receiver-driven "I hold up to T" handshake. Do NOT persist the watermark — it's the sender's guess about the receiver's state, and persisting it makes divergence permanent. [DONE in ffa20c6 (`fix(sync): pull content instead of pushing deltas`), which took the receiver-driven option and went past it: the per-peer/per-session watermark is *gone*, not merely unpersisted, so the "don't persist it" warning holds by construction. Push now advertises only a `dialog_tip`/`usage_tip` and the receiver derives `since` from the entries it actually holds at the moment of asking, so it cannot drift and a dropped fetch self-corrects on the next push. Usage got the missing catch-up endpoint, `GET /api/sync/usage?since=` (`sync::usage_since` + `RemoteUsageStore::newest_ts`) — the restart-only repair gap is closed for both stores. Verified 2026-07-31 against the real CHROME peer over Tailscale: 7554 usage records at since=0, 0 at since=<tip>, 682 dialog entries, and zero `non-contiguous dialog delta discarded` since the new build (201 historically, last 2026-07-24).]
- [x] 2026-08-27 12:59 — Two Claude Code instances sharing one cwd collide into a single dashboard row: the desktop app and a terminal session both in /Projects/landlord derive the same cwd-based chat_id. Last hook to fire wins the console_pids registration, so the terminal tab loses its status glyph and the row mixes two conversations. Needs a chat_id that distinguishes instances (agent_pid? session_id registry?) without fragmenting one conversation across a mid-session cd. [DONE, with the premise corrected. The row was NOT mixing two conversations: the desktop session's argv is `--session-id … --fork-session --resume <the terminal session's transcript>`, so it inherited that transcript wholesale (296 shared message ids, the terminal transcript silent after the fork) and one row showing one continuous thread was right. The console_pids half was fixed separately in a671196 (the hook's ancestor walk is bounded at its own claude process, so a tty-less desktop session writes no title at all rather than titling the tab that launched it). What was left was the destructive half — `AdapterOutput::Clear` called `remove_session(.., None, ..)`, i.e. unconditionally, so an exit or `/clear` in the abandoned tab would flush the merged dialog, stop the watcher and drop a row the live sibling was still using. Now gated by the pure `http_server::clear_permitted`: the row's owner is the `agent_pid` of whichever instance wrote it last (`liveness::AgentPids`, already overwrite-per-event), and a mismatched end signal is refused and logged `decision = "clear_ignored"`; unknown ownership on either side still defers to the end signal. Deliberately NOT done: distinguishing the chat_id per instance (a `landlord (2)` suffix or session_id-keyed rows). It would split a conversation the user forked on purpose, and the observed damage it would buy back is 8 title writes over 3 minutes in 84 days of widget.jsonl. `AgentPids`' own chat_id-keyed overwrite is likewise left alone — the reaper's unchanged-pid-and-quiet-`updated` guard already covers it, and a reaped row is recreated by the sibling's next event.]
- [x] 2026-08-27 15:24 — adjust usage based on subscription change [DONE, by adding a second unit rather than rescaling. The premise that history could be normalized turned out to be false: only percentages of the then-current quota were ever stored (1,169 local + 11,608 synced samples, all integer), and the API exposes nothing absolute — `limit_dollars` is null and no field names a plan — so every cross-era conversion would have been a stored percentage times an assumed ratio. The Max 5x→20x switch is visible in the data at 2026-08-27 15:10 (both counters zeroed while `seven_day_resets_at` held), and the 5h ratio is exactly 4.00x, but the 7d ratio is only fittable (~1.86, ±15%). Rejected: auto-detecting the boundary (19 zeroing signatures in 84 days, 18 of them ordinary resets) and rescaling history behind a hatch. Built instead a token-based intensity view from Claude Code's own transcripts — plan-independent by construction, so no future tier change or promo needs handling — behind a Percent|Tokens toggle, with the percentage chart untouched for the "am I burning my quota" question. The promo turned out to be a non-issue for the bars: it is weekly-only and started 2026-05-13, before the history begins. Token history syncs across devices on an append-`seq` watermark. Caveat: the token series only reaches back to transcript retention (2026-07-28 here), and earlier stretches are marked unknown rather than idle.]
- [ ] 2026-09-01 19:37 — Work intensity header collides below ~1280px — "Navigation: ← → one week" prints over the Percent/Tokens toggle. Repro: POST /api/window {"action":"resize","label":"intensity","width":1000,"height":620}; correct at 1280.
- [x] 2026-09-02 02:55 — re add sessions from agterm after restart [DONE: `session_restore.rs`, behind the same `terminals::TerminalAdapter` seam as the ready→idle trigger (new `sessions()` method; Windows returns `None` and restores nothing until its adapter exists). Two oracles, each asked only what it is authoritative for: `session_registry` for *is an agent alive here* (an agterm tab outlives the agent in it, since `session_launcher` starts `zsh -ilc 'claude; exec zsh -i'`), the tab title for *what was it doing and had you read it* (`Activity` is idle/busy only — verified live, `what-is-next` read `idle` there while its tab read `✋`). The tab title turned out to be the only durable copy of a row's status, and because titles are written after `apply_read_as_idle` it round-trips read-vs-unread too, so `attended_at` needed no persisting. `Working`/`Waiting` are the two statuses a stale glyph cannot be trusted for and survive only where the registry reports `Busy`. A tab with no title we recognize is skipped, which is also the anti-resurrection guard. Adversarial review found nine real defects, all fixed — notably that a bare `agtermctl tree` sees only the frontmost window, and that a tab can carry a glyph from a *previous* conversation, now closed by requiring `ChatIdRegistry` to have anchored one of the session's ids. Verified live: 10 live sessions / 0 rows → 10 restored, `what-is-next` back as BLOCK after 4h invisible, titles round-tripped byte-identically, later scans cost nothing.]
- [ ] 2026-09-02 04:58 — add note and links on creating firecrawl api key
- [ ] 2026-09-02 05:06 — use logarithmic scale for intensity graph
- [ ] 2026-09-02 17:12 — use limit reset day and time for weekly and daily usage calculations
- [ ] 2026-09-03 00:18 — A pinned Windows Terminal tab is invisible to attention: its caption never changes, so no EVENT_OBJECT_NAMECHANGE fires and departures to/from that tab go unobserved. (The second half of this entry described the caption-based pin detector, which no longer writes the stale flag at all; terminals::stale_check owns that now and catches the tab by a different route. The attention blindness above is the part that is still true.)
- [ ] 2026-09-03 00:51 — click on the status pill highlights it as a reminder to get back to it later
- [ ] 2026-09-03 03:02 — Fix the Homebrew tap: Casks/claude-code-dashboard.rb on AnotherSava/homebrew-tap main is a 0-byte file (blob e69de29, git's empty blob), so `brew install --cask AnotherSava/tap/claude-code-dashboard` cannot work; docs/pages/install.md names it as the primary macOS route. Broken since tap commit e9fbc3b ("chore: bump claude-code-dashboard to 1.9.1", 2026-08-24T07:24Z), which is +0/-92 on the cask; last good content was 4617 bytes at 747dd4e (the 1.9.0 bump). It has shipped broken through v1.10.0 and v1.11.0. ROOT CAUSE: bump-cask.yml's `Commit the bump` step passes `-f content="$(base64 -w0 "$CASK")"`, and the job had just moved to `runs-on: macos-latest` (line 26) so it could run `brew audit`. BSD base64 has no `-w`; run 32701261306 logs `base64: invalid argument` followed by the BSD usage line. A failing command substitution inside an argument word does not trip `set -euo pipefail`, so `gh api` PUT an empty `content`, got 200, and the job went green. WHY NOTHING REPORTED IT, three independent silences: (1) the job exits 0, and GitHub's Actions notifications fire on failure, so no mail; all four repository_dispatch runs (2026-08-24, 2026-08-28 x2, 2026-09-02) concluded success. (2) `Validate the rewritten cask` is gated `if: env.CHANGED == 'true'`, and CHANGED comes from `git diff --quiet` alone and never reads the cask's version, so an empty file is indistinguishable from a genuine no-op; run 33594205980 logs "cask already at 1.11.0, nothing to do" against a 0-byte file. Skipping looked like passing (feedback_not_run_is_not_pass). (3) The bump commit is pushed with GITHUB_TOKEN, so GitHub's recursion guard suppresses the tap's own Tests workflow; every Tests run in that repo is on a human push and none is on a bump commit, so the cask is never audited after the write. FIX, all in the tap repo: restore the cask body from 747dd4e and set version + sha256 for the release being cut; replace `base64 -w0 "$CASK"` with `base64 -i "$CASK" | tr -d '\n'` (or `openssl base64 -A`), which is runner-agnostic; assert the postcondition after the PUT by re-reading the committed file and failing unless it is non-empty and its `version` line equals $VERSION; and make the no-op branch grep the cask's version rather than trusting a quiet diff. The dashboard side needs no change: notify-tap.yml dispatches correctly and its TAP_DISPATCH_TOKEN worked on 2026-09-02. Verify by actually running `brew install --cask` afterwards, since a green tap run is exactly what failed to mean anything here. FIXED 2026-09-11, EXCEPT THAT LAST STEP. Tap commits 9a78bda (cask body restored from 747dd4e at v1.13.0, sha256 27c5db2c… verified by downloading the dmg and hashing it rather than trusting the release API's digest field) and e63fb66 (the workflow). The workflow fix is four changes, not one: `openssl base64 -A` replaces `base64 -w0`; every value is computed into its own assignment line, because `set -e` aborts on a failed command substitution there and does NOT when the same substitution sits inside an argument word — that shape, not the flag, is why the failure was silent; the no-op branch now reads the cask's parsed version instead of trusting `git diff --quiet`, and an empty or unparseable cask is a hard failure; `brew style`/`brew audit` run unconditionally rather than gated on CHANGED, which is what turned "skipped" into "passed"; and a final step re-reads the committed file from the API and fails unless it is non-empty and at the expected version. Also fixed a latent one nobody had hit: `sed -i -E` means different things on GNU and BSD and this job runs on macos-latest, so the rewrite now goes through a temp file. VERIFIED: the dry-run proved a 0-byte cask now fails at step 1 instead of no-opping green; a real dispatched run (34603908939) passed every step on macos-latest with `Validate the cask` executing under CHANGED=false, which the old workflow would have skipped; the tap's own Tests workflow passed; and origin/main holds byte-for-byte what was built. STILL UNVERIFIED, and it is the step this memo warned about: nobody has run `brew install --cask AnotherSava/tap/claude-code-dashboard` from a clean Mac. This machine is Windows. Do that from AIR before checking this off — a green tap run is exactly what failed to mean anything here, and `brew audit` parses the cask without ever installing it.
- [ ] 2026-09-03 04:07 — Add a post-release smoke check that the published artifacts actually install, for every release and both platforms. Today nothing does: a release is declared done when the CI run is green and the GitHub Release page has its assets, and neither fact touches the installers. That is the general form of the Homebrew tap failure (separate memo, 2026-09-03 03:02) rather than the same bug: that one is a specific broken cask plus a workflow that asserted nothing, and gets checked off when it is fixed; this is the standing check whose absence let it run for two releases. Shape to consider: a job after the release publishes, or a healthchecks.io-pinged scheduled run, that on macOS does `brew update && brew install --cask AnotherSava/tap/claude-code-dashboard` from a clean runner and asserts the app bundle exists with the expected CFBundleShortVersionString, and on Windows downloads the release .exe and asserts it is a valid NSIS installer of the expected version (a silent install plus a version probe if a runner allows it). Assert identity, not liveness: the check must confirm the version it just installed equals the tag, since "something installed" is exactly what a stale cask would also report. See ~/.claude/skills/heartbeat for the dead-man's-switch half if it becomes a scheduled job.
- [x] 2026-09-03 04:24 — The stale-tab detector misses the commoner rename. Measured live 2026-09-03: the achievement-overlay tab was renamed to "ttt" in Windows Terminal; the dashboard then made 99 successful SetConsoleTitleW writes to that session's console (all logged ok=true, title advancing through the context percentage), Windows Terminal ignored every one of them, and zero title_pinned decisions fired. Cause, and it is by design rather than a bug: observe_caption requires the caption to parse_title (a status glyph plus a name), so an arbitrary custom name is skipped before pin_verdict is ever reached. That skip is the same rule that stops a stray shell or another app's window being pinned onto whatever row looks closest, so it cannot simply be loosened. Net effect: the detector only catches the narrow case where the custom name still looks like a title we wrote, which is what you get by accepting the name Windows Terminal pre-fills and pressing Enter. That is the dangerous case (the tab keeps showing a status that has stopped being true) and it is worth having, but it is probably the rarer user action. Docs were corrected to describe both cases rather than promising a badge for either. PROBED 2026-09-03, and the first verdict of unfixable was wrong. A throwaway probe logging every WinEvent the pid-scoped hook receives (not just the OBJID_WINDOW ones on_event keeps) settled two things over 95s of real tab switching plus a live rename. (a) Windows Terminal raises NO tab-level MSAA name change: 20 events, of which exactly one was not OBJID_WINDOW, and that one was OBJID_CURSOR (id_object -9, hwnd 0, empty text). So reading per-tab titles off the existing hook is dead, and it needs no COM or UIA attempt to confirm that. (b) But the unrecognized caption IS delivered to us and IS attributable, which is what the original verdict got wrong. Captured: the caption read the working-glyph title we wrote for achievement-overlay at 23:17:36, then read ttt at 23:17:38, same HWND, with no EVENT_SYSTEM_FOREGROUND between them; thereafter achievement-overlay never appeared as a caption again while ttt appeared four times, and the console kept accepting writes with the row moving from Working to Blocked. Two usable signals, both on the code path already running and needing no new dependency: ADJACENCY, a caption going from a title we wrote to one we did not on the same window with no foreground change is that tab being renamed (ambiguous on a first sighting against switching to an already-renamed tab, which is exactly what the existing two-sample rule absorbs); and ELIMINATION, a row whose written title stops appearing as a caption at the moment an unrecognized caption starts appearing (needs history, and is confounded by a tab simply never visited, so it corroborates rather than accuses). Real limits either way: both need the tab visited at least once, as today's detector does, and a session in a background window or a non-WT terminal never appears as a caption at all, so absence can never accuse on its own. The docs shipped in v1.12.0 say this case is not flagged, so they need revising too if this is built.
- [ ] 2026-09-03 19:32 — add parallelism level graph ie how many different agents have worked within last hour or shorter time
- [ ] 2026-09-05 23:08 — Fix the relay's unroutable reply address, and the two things that made it cost three sessions a wrong diagnosis. Reported by the what-is-next session on CHROME via the peer relay 2026-09-05, independently reproduced by the trips and printlab sessions on AIR; every claim below re-verified against this tree before recording. THE BUG: http_server.rs post_message mints reply_to as format!("{this_device}/{from_agent}") unconditionally, with no separator check, and from_agent is only trimmed and emptiness-filtered where it is read. So a caller passing from_agent="CHROME/what-is-next" gets reply_to="CHROME/CHROME/what-is-next", which the far side then refuses. Confirmed end to end on the wire, two consecutive messages from one session to one peer with from_agent the only variable: routing block went CHROME/CHROME/what-is-next then CHROME/what-is-next, and the claim header's agent went "CHROME/what-is-next" then "what-is-next" - two independent signals corrected by one field. DO NOT CHANGE THE RECEIVER: sync.rs passes reply_to: env.reply_to.as_deref(), carrying the value rather than deriving it, and that is deliberate - from_id lowercases and collapses to [a-z0-9-] while resolve_message_target compares exactly, so a receiver reconstructing the address would produce a confident wrong one. WHY THE MISTAKE IS INVITED: GET /api/agents returns two formats under one key. agent_roster sets id: s.id.clone() raw, so a local row is the bare project ("what-is-next") and a remote row is namespaced ("AIR/what-is-next"), while the stripped project is computed beside it and used elsewhere. Looking yourself up in another machine's view of the roster - the natural way to learn what to call yourself - therefore hands you the qualified form, and target IS namespaced while from_agent is NOT. The asymmetry is invisible until a reply bounces. WHY IT COST SO MUCH: an unroutable address and an absent session both land on InboxLookup::NotFound, so both answer 404 no_such_session with the identical sentence "no live interactive session on this machine derives that project id". Four failed sends and two wrong statements to Oleg that a live session had ended, when it had been up throughout; three sessions concluded the other machine's relay was at fault before anyone read http_server.rs. One correction to the report as received: ambiguous_target is already distinct - sync.rs answers it 409 with its own wording about several sessions sharing a directory - so the conflation to fix is specifically unroutable-versus-absent inside no_such_session, not a three-way tie. FIXES, ranked: (1) refuse a from_agent containing "/" with a receipt naming the bare form, since the caller is on the same machine and the correction can be immediate and specific; (2) stop labelling two shapes "id" in /api/agents, or add an explicit self_id so a session never infers its own name from a peer's view; (3) split the refusal reasons so an unroutable address does not read as a dead peer - same-wording-different-cause is what turned a one-character mistake into a wrong diagnosis twice; (4) change nothing in the receiver. Ambiguity has no disambiguator either: with two live sessions in one directory the relay refuses to pick and offers no ref syntax, so both "project [ref]" and "project#ref" fail.
- [ ] 2026-09-08 03:09 — Fix the snapshot-to-publish race in commands::emit_sessions_updated that leaves a finished row showing WORK. OBSERVED 2026-09-08T04:44:04Z on bga-assistant (console pid 32604): a turn ended, the row went Done, and both the widget row and the terminal tab showed blue for 4m41s until the user read the session. LOG EVIDENCE, all within 13ms, from widget.jsonl: .494 event -> set decision=classify status=Done; .500 apply_set prior_status=Working new_status=Done; .507 terminal title written title="green-circle bga-assistant"; .507 terminal title written title="blue-circle bga-assistant"; .856 event sessions_updated x2. Note the two title writes in one millisecond and the two emits - the stale blue one lands SECOND and wins. MECHANISM: emit_sessions_updated takes its snapshot with display_snapshot at the top of the function and only then calls terminal_title::sync, lid_awake::sync, tray_badge::refresh and app.emit, holding no lock across that pair. Two callers ran concurrently on that turn - the Stop hook path (http_server -> set -> emit) and the log_watcher, which fires moments later when Claude Code flushes the final assistant message, a lag the design documents and relies on. The watcher snapshotted BEFORE apply_set at .500 and so was carrying Working; the hook path snapshotted after and was carrying Done. They then serialise INSIDE terminal_title::sync on its own pids/last mutexes and inside push_title on the console attach lock, so the two writes are ordered - but each still carries the snapshot it took earlier, so the later WRITE won with the earlier READ. The two sessions_updated payloads at .856 have the same defect, which is why the widget row was blue and not just the tab glyph. WHY IT PERSISTED: terminal_title::sync runs only from emit_sessions_updated and nothing calls that on a timer, so with no further state change on the row nothing re-asserted the title; every attention poll from 04:44:15 to 04:48:45 read back the blue title. REASSERT_MS does not help because it only applies once sync is called. It corrected only when the read at 04:48:45 triggered the next emit, which wrote the white idle glyph. WHAT WAS NOT WRONG, and worth stating so nobody widens the hunt: AppState held Done throughout, provable because attention_seen fired at 04:48:45 and the read flip applies only to a row whose attention verdict is pending, which requires Done. Notifications, GET /api/agents and the sync push were all correct; the corruption was display-only. This is a general race at every turn end rather than anything specific to this agent - it only becomes visible when the watcher's emit loses the ordering coin flip, which is why it is rare and why a clean log is not evidence of absence. FIX: move the snapshot inside a mutex that also covers the sync calls and the emit, so snapshot-to-publish is atomic per call; two concurrent callers then each publish a self-consistent view and the later one wins with the newer state. It cannot deadlock - nothing inside the emit path re-enters emit_sessions_updated, and stale_check::request is a non-blocking channel send. REJECTED ALTERNATIVE: stamping each write with the row's updated field and having sync refuse a staler snapshot fixes the tab glyph but not the frontend emit, which carried the same stale payload and is half the symptom. ALSO CHECK emit_sessions_updated_remote, which has the same shape - snapshot taken outside any lock - though it only emits and writes no title.
- [ ] 2026-09-08 04:01 — Docs site builds from an unpinned remote_theme in docs/_config.yml, so GitHub Pages serves whatever it last cached and the theme can change under the site with no commit here. Pin it: remote_theme: just-the-docs/just-the-docs@<current release, v0.12.0 as of 2026-09-08, re-resolve when doing it>. The attribution footer is already suppressed here by _includes/nav_footer_custom.html, so the pin is the only thing missing. Detail in the github-pages skill, section Auditing a site that already exists. Found 2026-09-08 auditing six local docs sites.
- [ ] 2026-09-09 01:18 — Move the terminal title WRITE onto the TerminalAdapter seam, so the seam owns writing as well as reading. Today terminal_title::push_title has two #[cfg] arms - SetConsoleTitleW under a console attach on Windows, an OSC 0 escape to the controlling tty elsewhere - and it is the last terminal-specific logic living outside terminals/. It is NOT a cross-platform bug: cfg is compile-time, both arms exist, and CI builds the [windows-latest, macos-latest] matrix on every push, so both platforms work today. Two things are wrong with it anyway. (1) Platform is the right axis only by luck: SetConsoleTitleW writes the console object and the terminal merely renders it, so one mechanism covers Windows Terminal, VS Code's terminal and bare conhost alike, and OSC 0 to a tty likewise covers agterm, Terminal.app and iTerm - but the day a platform needs two different write mechanisms for two terminals, this shape cannot express it. (2) It carries a verification blind spot: a Windows-only cargo check cannot see a break in the cfg(not(windows)) arm, which is exactly how the macOS build was broken once in this feature's history and caught only by a reviewer. The shape is already there: TerminalAdapter::attached_surface(pid) was given a pid parameter precisely so a terminal whose write target is not ambient process state could implement it, and a write method would take the same pid. Do this as its own change with its own review, not folded into a diff about something else.
- [ ] 2026-09-09 01:41 — Decide where the device chip belongs on a session row, now that every badge has moved inside the .name box. PROMPTED BY a real confusion on 2026-09-07, asked twice and only answered on 2026-09-09: the dashboard showed two what-is-next rows and the reason was correct behaviour, not a bug. CHROME was running its own what-is-next while remote_history/AIR.json held AIR/what-is-next, so the widget rendered one local row and one synced row for two genuinely different sessions on two machines. Ruled out with evidence rather than reasoning: name_shared_by was never stamped above 1 across 8MB of widget.jsonl, so no row was merging sessions. THE PROBLEM is that both rows print the same name and the only thing distinguishing them is the device chip, which sits between the name and the state pill - so at a glance the duplicate reads as a dashboard fault rather than as two machines. THE TENSION is with the rule this session established and wrote into CLAUDE.md and SessionItem.svelte: every badge rides inside .name and the state pill is only ever the status, because a mark after the pill reads as a qualifier on the state, which is the one thing about these rows not in doubt. Which machine a session runs on is squarely a fact about the session, so by that rule the chip belongs with the name; it is outside it only because nobody looked. OPTIONS: (1) move .device inside .name after the id and before the badges, so a synced row reads as one label - what-is-next AIR - which is the change the confusion argues for, and .device already has flex-shrink so no CSS work is needed; (2) leave it and accept the rule has one unstated exception, which the code comments now name explicitly so it cannot be misread; (3) something else for the duplicate-name case specifically, since the chip is only load-bearing when two rows share a name and is redundant noise the rest of the time. Oleg said the current placement is ok and wanted this parked for a fuller discussion rather than changed now.
- [ ] 2026-09-09 07:04 — Rework the docs screenshots: the Windows half is done, the macOS half is not. BUILT 2026-09-10: every figure now carries one variant per OS (`docs/_includes/figure.html`, `os_chip.html`, `head_custom.html`, `_sass/custom/custom.scss`, `_data/platforms.yml`), files renamed to `<figure>-<platform>.png`, nine manifest entries (one per picture that must be re-taken, since a capture script runs on one machine), Windows capture scripts under `docs/screenshots/capture/`, and `check-figures.py` in CI reconciling pages, disk, manifest and README. All four Windows variants captured: hero, compact-mode, terminal-tabs (a Windows Terminal tab strip, which is a different application from agterm's sidebar, not the same UI on another OS), history-window, work-intensity. CORRECTION to this memo's original audit: `compact.png` was a **macOS** capture, not Windows — its device pills read CHROME, so the capturing machine was the other one, and its corner alpha ramp matches the other macOS frames. The probe that got it wrong looked for traffic lights on an undecorated window and matched the DONE pill's green. So only one of the five committed frames was ever Windows. STILL TO DO, all of it on the Mac: write the four macOS capture scripts; re-shoot `history-window-macos` and `work-intensity-macos` to clear the transparent one-pixel corner ring and `terminal-tabs-macos` to clear the blue bleed pixel (both measured, both recorded in those entries' capture steps); re-shoot `compact-mode-macos`, which shows the private project name `what-is-next`; capture `hero-macos`, the only figure still printing "The macOS screenshot hasn't been taken yet" on the site; then swap the README's single hero embed for the two-column table. Every session visible in any frame must be a public repo.
- [ ] 2026-09-10 05:52 — Show on the tray icon when an agent needs the machine itself. New global guideline (`~/.claude/CLAUDE.md`, "Taking Over the Machine"): an agent that has to raise a window, move the pointer, synthesize a keystroke or otherwise drive the GUI must ask immediately before each burst, and the dashboard must make that state visible without reading the transcript. Three states, each legible at tray size: normal; a distinct variant while a request is **pending**, so a user away from the chat can see an agent is waiting on them; and a distinct **animated** variant while the interaction is running, so a machine moving on its own is never ambiguous. Back to normal the moment the burst ends, INCLUDING when it fails or is declined — an icon stuck on "interacting" trains the user to ignore it. SHAPE: a loopback route beside `/api/window` for an agent to declare and clear the state (it is a claim about the caller, so it needs no cross-machine half); the state on `AppState` rather than stamped at emit time, since `tray_badge::refresh` must be able to read it; two render paths alongside the existing `lights`/`number` styles in `tray_badge.rs`, which already composes variants of the app icon and already has an alert overlay to borrow the geometry from; and a tick for the animation, which is the one genuinely new machinery here — every other tray repaint today is edge-driven from `emit_sessions_updated` / `emit_usage_limits_updated`. Open questions worth settling before building: whether the pending state should also fire a Telegram ping (the user may be away from the machine entirely, which is exactly when a pending request matters most), and how it composes with the existing usage badge and context-alert border, which already own the icon.

- [x] 2026-09-10 07:52 — make sure that app restarts do not cause extra limit usage api calls (done 2026-09-11: a launch fired TWO requests — `request_refresh` raced the opening poll and `Notify` parked a permit redeemed the instant it returned — and then every launch fired one more. Both are closed: the refresh gate now measures against poll attempts rather than the reading's age, and `poll_once` skips its opening poll outright when `usage_cache.json` holds a reading younger than the interval, replayed as `Ok` because it is no staler than what a running instance would be showing. `usage_cache.rs` also persists the endpoint's own `Retry-After` so a deadline survives the restart that used to reset it. Observed live: `opening poll skipped; the stored reading is younger than the interval age_ms=60876`.)
- [ ] 2026-09-11 04:55 — Relay the macOS screenshot errand to the session on AIR, AFTER this work is pushed (it needs the committed capture scripts, the figure include and the manifest in its checkout, so relaying before the push hands it a tree that cannot run any of it). The errand itself is the STILL TO DO list in the 2026-09-09 07:04 memo, which that session should read from the repo rather than from the message: write the four macOS capture scripts, re-shoot history-window-macos and work-intensity-macos to clear the transparent one-pixel corner ring and terminal-tabs-macos to clear the blue bleed pixel, re-shoot compact-mode-macos (it shows a private project name), capture hero-macos, then swap the README's single hero embed for the two-column table. Three things settled on the Windows side that the macOS half must match rather than re-decide: every session visible in any frame must be a PUBLIC repo; a re-shoot keeps the subject of the shot it replaces (now a general rule in the documentation skill), so history-window-macos stays on bga-assistant and work-intensity-macos moves to the week of Aug 31 - Sep 6, 2026, which is what work-intensity-windows shows and which its own Aug 10 - Aug 16 currently contradicts; and the corner halo is trimmed by `docs/screenshots/capture/lib/trim_halo.py`, which the Windows scripts run automatically and a macOS re-shoot must run by hand. Send via the peer relay; draft the message, show it, and only send on an explicit yes — it is outward communication to another agent.
- [ ] 2026-09-11 04:55 — Run the silence experiment on the Anthropic usage endpoint: stop polling entirely for 60 minutes, then make exactly ONE request with every response header captured. WHY: the open question from 2026-09-10 is why it takes HOURS to get out of the limiter when the one Retry-After ever observed was 832 seconds (~14 min). Two explanations fit everything seen so far and they need different fixes — either the window is genuinely long and Retry-After only ever reports the tail of it, or each request made while blocked extends it, in which case the 10-minute poll was renewing its own punishment indefinitely and the block-skip now shipped in usage_limits::poll_once already fixes it. A single request after a long verified silence separates them: if it succeeds, the window was being extended; if it is refused with a fresh multi-hour Retry-After, it was long all along. WHAT IS ALREADY IN PLACE: poll_once skips while `usage_cache.json` carries a live `blocked_until` and replays the stored sample so the bars keep a real figure (both observed live 2026-09-11), and the client now reads response headers before consuming the body, which is what made the 832 visible at all. HOW TO GET THE SILENCE: the app polls on a timer, so quit it rather than trying to hold it off, and do not run `deploy` during the hour — every deploy is a restart and a restart polls at the top of its loop. Record the full header set either way; the endpoint is undocumented and this is the only way its policy becomes knowable. EVIDENCE ADDED 2026-09-11 05:01, from a real 429 rather than a planted one: the block it set ran ~16 minutes (`in_ms` counted down 986182 -> 906502 across three skipped polls), which is the same order as the 832s seen on 2026-09-10 and nothing like the hours the question is about — so either the hours are many consecutive ~15-minute windows, each renewed by the next request, or the long refusals have a different trigger entirely. The first reading is the one the experiment tests. WHAT TRIPPED IT is also worth recording, because it is the remaining half of memo 1 and is NOT fixed: the app polls once at the top of its loop on every start, and a screenshot session that deploys and restarts the app six times in ten minutes therefore makes six requests no rate limiter will forgive. `request_refresh` now declines the *second* request per launch and `poll_once` skips while blocked, but neither touches the one-per-start. BUILT 2026-09-11, so the experiment starts from a cleaner baseline than the one that produced tonight's 429: a start now replays a sample younger than the poll interval and skips the opening poll, so a deploy costs zero requests, and a live deadline decides the sleep so a blocked poller no longer wakes every minute. The hours question is therefore still open on its own terms rather than confounded by our own request rate.
- [ ] 2026-09-13 08:30 — A relayed peer message becomes the receiving row's LABEL, and its whole envelope is stored as a prompt. The label then reads `[cross-machine message, relayed by the dashboard] Sender: agent "..."` — transport boilerplate where the task should be — and `prompt_history.json` holds the envelope verbatim, one of them 34KB. Measured on this machine 2026-09-13: 39 stored relay envelopes across four rows (tauri-dashboard, transcripts, what-is-next, and one on the Mac side). FOUND BY the AIR session, which reverted a hero re-shoot because a row it had created by messaging a peer showed this in frame. THERE ARE TWO ENVELOPE FORMS AND ONLY ONE IS HARMLESS — corrected 2026-09-13 after AIR pushed back, and the back-and-forth is worth keeping because both of us were half right. (a) The CROSS-MACHINE RELAY (`[cross-machine message, relayed by the dashboard]`, 43 here) carries NO socket path: its reply address is the loopback `POST http://127.0.0.1:9077/api/message`, and `messagingSocketPath` is resolved inside the receiving dashboard (`session_registry::inbox_for`) and never reaches the agent's text. (b) Claude Code's OWN LOCAL `SendMessage` between sessions on one machine is a different transport with a different preamble — `<cross-session-message from="uds:..." from-name="...">` — and it DOES put the sender's inbox address in the `from` attribute. Confirmed on this machine, `tauri-dashboard` dialog entry 990: `from="uds:\.\pipe\LOCAL\cc-msg-de0e9ecbe8fad7d35bb0cf6c4cc1d41e"`, the Windows named-pipe form; the macOS form is `uds:/tmp/cc-socks/<pid>.sock` and arrives here through dialog sync. Calibrate the alarm: the pipe name is an ADDRESS, not a credential — writing to that inbox needs the per-session `<pid>.<64hex>.key` file, which is not in the envelope — so this is internal plumbing in plain view rather than an exploitable secret. My first pass reported "no socket path" because I searched only inside relay envelopes, which was true and too narrow; AIR's first pass attributed the leak to the relay, which was the wrong transport. So the leak is mild and the LABEL is the real defect. WHY IT MATTERS BEYOND LOOKS: the label is what the widget shows, what `terminal_title::build_title` would carry, what the notification text builders read, and what the dialog sync pushes to every peer — so one relayed message puts several KB of routing boilerplate on all of those surfaces, and any screenshot containing such a row prints it. `docs/screenshots/screenshots.json` says nothing about this today, and the hero's staging steps are exactly where it would bite again. SHAPE OF A FIX, unsettled: the envelope is assembled by `peer_message::build_content` and arrives as an ordinary `UserPromptSubmit`, so the dashboard cannot currently tell it from a human prompt — the honest fix is probably for the adapter to recognise the `----- BEGIN RELAYED MESSAGE {nonce} -----` fence it already mints and label the row with the sender plus the fenced body, keeping the full envelope in the dialog where the history window can show it. Do NOT simply truncate: the first 80 characters are the least informative part of the whole string.
