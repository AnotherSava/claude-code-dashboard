{
  "screenshots": [
    {
      "id": "hero-windows",
      "files": [
        "hero-windows.png"
      ],
      "shows": "Windows. Main widget, one row blocked on its user, one working and one finished, with the 5h and 7d usage bars in the header — the hero image on both the README and the docs index",
      "policy": "confirm",
      "capture": {
        "command": "pwsh -File docs/screenshots/capture/hero-windows.ps1",
        "steps": [
          "Windows. The macOS half of this figure is hero-macos, which is its own entry and its own script.",
          "Deploy the current build so the shot matches the working tree, not the installed release",
          "Stage a real spread: one session genuinely blocked on its user, one mid-turn, one finished. The dashboard's hook does not fire on PostToolUse, so a session busy inside a long turn does not refresh its row — the working row has to be one that is mid-turn right now.",
          "Sessions live in public repositories only. A row's label is its actual prompt, and a private project's task text ends up in the frame verbatim.",
          "Turn sync listening off (`sync.listen`, start-only) so a peer's rows are not in frame; the script refuses while a peer is connected.",
          "Turn `restore_sessions` off and restart, so the widget starts empty and only the staged sessions get rows. With it on, every idle session the terminal can vouch for comes back and the frame is whatever happened to be running.",
          "Both usage bars must have a reading. Restarting the app repeatedly trips the usage endpoint's 429 backoff and the bars then render `--%`; this is the only frame that shows the header, so wait for a poll that succeeds after the restarts.",
          "Captured with -Method Alpha: the window is photographed over a black and a white backdrop and the alpha solved per pixel, so the frame carries the real border and rounded corners with everything outside them transparent. Do not add -Client; the script refuses the combination."
        ]
      },
      "verifiedAt": "d0e142c"
    },
    {
      "id": "compact-mode-windows",
      "files": [
        "compact-mode-windows.png"
      ],
      "shows": "Windows. Main widget in compact mode: rows reduced to name and state badge, and the usage bars reduced to their percentage and reset time",
      "policy": "confirm",
      "capture": {
        "command": "pwsh -File docs/screenshots/capture/compact-mode-windows.ps1",
        "steps": [
          "Windows. The macOS half of this figure is compact-mode-macos.",
          "Deploy the current build",
          "The script writes `compact_mode`, captures, and puts it back. It writes the file whole and then touches it: the config watcher reads on the first notify event and debounces for 150ms, so a truncate-then-write hands it a half-written file and every setting silently falls back to its default.",
          "This frame carries no device pill, because a peer's rows would put that machine's project names in the picture. The macOS variant is the one that shows sync, and it can: this device's own project names are public.",
          "Captured with -Method Alpha: the window is photographed over a black and a white backdrop and the alpha solved per pixel, so the frame carries the real border and rounded corners with everything outside them transparent. Do not add -Client; the script refuses the combination."
        ]
      },
      "verifiedAt": "d0e142c"
    },
    {
      "id": "compact-mode-macos",
      "files": [
        "compact-mode-macos.png"
      ],
      "shows": "macOS. Main widget in compact mode: six rows, no label lines, showing DONE/WORK/IDLE badges and device pills on the three rows synced from another machine",
      "policy": "confirm",
      "capture": {
        "steps": [
          "macOS. The Windows half of this figure is compact-mode-windows.",
          "Deploy the current build",
          "Turn on compact mode so the label lines are suppressed",
          "Have both local and synced rows live, so at least one device pill is in frame",
          "Capture the window rect",
          "This frame was misfiled as a Windows capture until 2026-09-09. Two independent readings say otherwise: its device pills read CHROME, so the capturing machine is the other one; and its corner alpha ramp is byte-for-byte the shape terminal-tabs-macos has and hero-windows does not. The audit that got it wrong probed the top-left for macOS traffic-light hues, which cannot work on an undecorated window — it matched the DONE pill's green and nothing else.",
          "The compositor's drop shadow is trimmed from the four corners by `python docs/screenshots/capture/lib/trim_halo.py <png>` — a flood fill from each corner over pixels that are faint or black, bounded to a 48px box, so the window's own translucent border survives. Windows captures run it automatically; a macOS re-shoot must run it by hand or the halo comes back."
        ]
      },
      "verifiedAt": "5c248d5"
    },
    {
      "id": "terminal-tabs-windows",
      "files": [
        "terminal-tabs-windows.png"
      ],
      "shows": "Windows. Windows Terminal's tab strip, each tab carrying its session's status glyph written by the dashboard",
      "policy": "confirm",
      "capture": {
        "command": "pwsh -File docs/screenshots/capture/terminal-tabs-windows.ps1",
        "steps": [
          "Windows. This is not the same picture as terminal-tabs-macos: there is no session sidebar on Windows, the same information rides the tab strip. The two are a pair for the feature, not for the UI, and the docs give each its own caption naming the application.",
          "Stage the sessions in a terminal window of their own (`wt -w <name> new-tab`) so the strip carries only those tabs — every other tab open on the machine would otherwise be in frame, private names included",
          "Several sessions across projects showing a spread of glyphs",
          "The script hides the widget for the shot: it is always-on-top, and this is the one frame that has to be read off the desktop because PrintWindow returns a stale surface for Windows Terminal's XAML islands",
          "Both crops are measured, not hardcoded — the strip's height from where the flat bar colour ends, and its width from the last tab before a long empty run, so the window controls at the far right stay out"
        ]
      },
      "verifiedAt": "d0e142c"
    },
    {
      "id": "terminal-tabs-macos",
      "files": [
        "terminal-tabs-macos.png"
      ],
      "shows": "macOS. agterm's sidebar with per-session status glyphs written by the dashboard, two of them carrying a context-usage suffix",
      "policy": "confirm",
      "capture": {
        "steps": [
          "macOS with agterm. The Windows half of this figure is terminal-tabs-windows, and it is a different application rather than the same one on another OS.",
          "Several sessions across projects showing a spread of glyphs, and at least one past the context-percent threshold so a [N%] suffix is visible",
          "Capture agterm's sidebar; the surrounding chrome is third-party and is not evidence for staleness",
          "Fixed 2026-09-10 without a re-shoot: the blue bleed pixel at the top-left (RGB 57,142,198, alpha 9) was part of the corner halo and trim_halo.py cleared it.",
          "The compositor's drop shadow is trimmed from the four corners by `python docs/screenshots/capture/lib/trim_halo.py <png>` — a flood fill from each corner over pixels that are faint or black, bounded to a 48px box, so the window's own translucent border survives. Windows captures run it automatically; a macOS re-shoot must run it by hand or the halo comes back."
        ]
      },
      "verifiedAt": "5c248d5"
    },
    {
      "id": "history-window-windows",
      "files": [
        "history-window-windows.png"
      ],
      "shows": "Windows. History window for one session: timestamped prompts and replies with collapsed middles, under a native Windows title bar",
      "policy": "confirm",
      "capture": {
        "command": "pwsh -File docs/screenshots/capture/history-window-windows.ps1",
        "steps": [
          "Windows. The macOS half of this figure is history-window-macos.",
          "SUBJECT: the bga-assistant session (AnotherSava/bga-assistant, public), the same conversation history-window-macos photographs — the two variants are one figure and must not show two different sessions. Row id here is `assistant`, the directory basename, since projects_root is unset on this machine; the window title is its custom name, bga-assistant.",
          "SCROLL POSITION: set by -Height, because the list is bottom-anchored and cannot be scrolled without driving the machine. 310 is measured against the content — one line higher puts an absolute path from a reply in frame, one line lower drops the <...> fold the figure exists to show.",
          "Deploy the current build",
          "Read the tail before capturing. prompt_history.json in the app data dir holds every stored dialog and is quicker to search than opening windows; the requirements a session has to meet are in the script's header.",
          "Probe with -Method PrintWindow first — it needs no foreground, so it costs no permission to take over the machine — then take the Alpha shot once",
          "The window's title is the session's display name, not the word History, which is what the capture matches on",
          "The compositor's drop shadow is trimmed from the four corners automatically by lib/trim_halo.py"
        ]
      },
      "verifiedAt": "d0e142c"
    },
    {
      "id": "history-window-macos",
      "files": [
        "history-window-macos.png"
      ],
      "shows": "macOS. History window for one session: timestamped prompts and replies with collapsed middles",
      "policy": "confirm",
      "capture": {
        "steps": [
          "macOS. The Windows half of this figure is history-window-windows.",
          "Deploy the current build",
          "Open history for a session with enough turns that at least one reply is folded behind a <...> button",
          "Capture the history window rect",
          "Known defect, measured 2026-09-09, to fix on the next re-shoot: a fully transparent one-pixel ring inside the rounded corner at all four corners. Sampling the top-left diagonal gives alpha 243, 163, 0, 99, 255 — the 0 at the eighth pixel is the hole, and it is what makes the corners read ragged against a light page.",
          "The compositor's drop shadow is trimmed from the four corners by `python docs/screenshots/capture/lib/trim_halo.py <png>` — a flood fill from each corner over pixels that are faint or black, bounded to a 48px box, so the window's own translucent border survives. Windows captures run it automatically; a macOS re-shoot must run it by hand or the halo comes back."
        ]
      },
      "verifiedAt": "5c248d5"
    },
    {
      "id": "work-intensity-windows",
      "files": [
        "work-intensity-windows.png"
      ],
      "shows": "Windows. Work intensity window, Days view in Percent unit, one week of 10-minute bars with the full-5h-pace reference line, under a native Windows title bar",
      "policy": "confirm",
      "capture": {
        "command": "pwsh -File docs/screenshots/capture/work-intensity-windows.ps1",
        "steps": [
          "SUBJECT: the week of Aug 31 – Sep 6, 2026. Pinned by date (-WeekStart), not by the -Offset the window's API takes, because an offset is relative to the run date and would photograph a different week every week while looking like a constant. A re-shoot keeps this week: the prose beside the figure was written against its pace line and its over-2x red bars.",
          "The macOS half still shows Aug 10 – Aug 16 and should move to this week when it is re-shot — a paired figure is read as two pictures of one thing.",
          "Windows. The macOS half of this figure is work-intensity-macos.",
          "Deploy the current build",
          "1520 logical px wide. Below roughly 1280 the header controls collide, which is a separate open defect; at 1320 they merely wrap onto two lines, and 1520 is the first width that keeps the date range, Navigation and Legend on one line.",
          "The script parks the mouse pointer clear of the window: hovering a bar raises a tooltip that then sits in the committed frame"
        ]
      },
      "verifiedAt": "d0e142c"
    },
    {
      "id": "work-intensity-macos",
      "files": [
        "work-intensity-macos.png"
      ],
      "shows": "macOS. Work intensity window, Days view in Percent unit, one week of 10-minute bars with the full-5h-pace reference line",
      "policy": "confirm",
      "capture": {
        "steps": [
          "macOS. The Windows half of this figure is work-intensity-windows.",
          "Deploy the current build",
          "Open Work intensity from the tray, Days view, Percent unit",
          "Pick a week with enough activity that the pace reference and the over-2x red bars both appear",
          "Size the window at or above 1280px wide — the header controls collide below that",
          "Capture the window rect",
          "Known defect, measured 2026-09-09, to fix on the next re-shoot: the same fully transparent one-pixel corner ring as history-window-macos, alpha 243, 163, 0, 99, 255 down the top-left diagonal",
          "The compositor's drop shadow is trimmed from the four corners by `python docs/screenshots/capture/lib/trim_halo.py <png>` — a flood fill from each corner over pixels that are faint or black, bounded to a 48px box, so the window's own translucent border survives. Windows captures run it automatically; a macOS re-shoot must run it by hand or the halo comes back."
        ]
      },
      "verifiedAt": "5c248d5"
    }
  ],
  "declinedGaps": [
    {
      "id": "G1",
      "where": "docs/pages/features.md — Notifications",
      "would_show": "A real Telegram ping, so the section shows what the message looks like and what it carries",
      "declined": "2026-09-02"
    }
  ]
}
