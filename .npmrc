# npm treats `engines` as advisory: it prints a warning into the install output
# and installs anyway, so this repo's `engines.node`, its `.nvmrc` and CI's
# node-version can all agree while somebody quietly builds on another runtime.
# This makes the mismatch an EBADENGINE failure instead.
engine-strict=true
