{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all application windows",
  "windows": ["main", "history", "about", "intensity"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
